# دليل استخدام نظام التراجع والإعادة (Undo/Redo)

## الميزات المضافة:

### 1. أزرار التراجع والإعادة
تم إضافة زرين في أعلى الصفحة:
- **زر التراجع (Undo)**: باللون الأصفر - للتراجع عن آخر تعديل
- **زر الإعادة (Redo)**: باللون الأزرق - لإعادة آخر تعديل تم التراجع عنه

### 2. كيفية العمل:

#### التراجع (Undo):
1. عند تعديل أي خلية حضور، يتم حفظ الحالة السابقة في قاعدة البيانات
2. الضغط على زر "تراجع" يعيد آخر تعديل قمت به
3. يمكنك الضغط عدة مرات للتراجع عن عدة تعديلات متتالية
4. يتم تحديث عداد الراحات تلقائياً

#### الإعادة (Redo):
1. بعد التراجع عن تعديل، يمكنك إعادته بالضغط على زر "إعادة"
2. يمكنك إعادة عدة تعديلات متتالية
3. ملاحظة: عند إجراء تعديل جديد، يتم مسح سجل الإعادة

#### القائمة المنبثقة (Right-Click):
- يمكنك أيضاً الضغط بالزر الأيمن على أي خلية واختيار "تراجع عن آخر خطوة"

### 3. التحديثات التقنية:

#### قاعدة البيانات:
- `AttendanceChangeLog`: لحفظ التغييرات السابقة (للتراجع)
- `AttendanceRedoLog`: لحفظ التغييرات المُلغاة (للإعادة)

#### APIs الجديدة:
- `/api/undo-last-change/`: للتراجع عن آخر تعديل
- `/api/redo-last-change/`: لإعادة آخر تعديل تم التراجع عنه

### 4. ملاحظات مهمة:
- التراجع والإعادة يعملان على مستوى المستخدم (كل مستخدم له سجل خاص)
- عند إجراء تعديل جديد، يتم مسح سجل الإعادة لمنع التضارب
- يتم تحديث عداد الراحات تلقائياً مع كل تراجع أو إعادة
- التحديثات تظهر فوراً في الواجهة دون الحاجة لإعادة تحميل الصفحة

### 5. الأخطاء المتوقعة:
- "No actions to undo": لا توجد تعديلات للتراجع عنها
- "No actions to redo": لا توجد تعديلات للإعادة
